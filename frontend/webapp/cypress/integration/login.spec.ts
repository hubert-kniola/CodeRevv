/// <reference types="cypress" />

const URL = `http://localhost:3000/signin`;

describe('Login page', () => {
    cy.visit(URL);

    it('fills inputs', () => {
      
    });
  });
  
  export {};
  